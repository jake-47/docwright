+++
title = "first note"
date = 2026-06-27
+++

This post lives inside the `notes` section. On the home page, `notes` shows up as a single link rather than expanding, and you reach this post by clicking through to the section's own page first.

Turn on `show_breadcrumbs` in `config.toml` and you will see a `home > notes > first note` trail at the top, which is the navigation that makes deeply nested sections usable.

Delete the `notes/` folder when you start writing your own.
